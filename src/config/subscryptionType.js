const streamOnLine = {
    type: "stream.online",
    version: "1"
}

const streamOffLine = {
    type: "stream.offline",
    version: "1"
}

const chanelUpdate = {
    type: "channel.update",
    version: "2"
}

module.exports = {streamOffLine, streamOnLine, chanelUpdate};